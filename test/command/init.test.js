'use strict';

// const command = require('../command');
// const path = require('path');
// const fs = require('fs');
// const spawn = require('child_process').spawn;
// const exec = require('child_process').exec;
// const { promisify } = require('util');
// const readFile = promisify(fs.readFile);
// const dara = path.join(__dirname, '../bin/dara.js');
// const execNode = process.env.npm_node_execpath || process.env.NODE || process.execPath;
describe('init command should ok', function () {
  it('init a dara file should be ok', async function () {
    // const a = await command.dara(['init']);
    // let opts = {
    //   env: process.env,
    // };
    // let cmd = [dara, 'init'];
    // let child = spawn(execNode, cmd, opts);
    // child.stdout.on('data', (chunk) => {
    //   if (chunk.toString() === 'package scope: ') {
    //     console.log('stdin write');
    //     child.stdin.write('test');
    //   }
    // });
  });

});